rough <- read.table("household_power_consumption.txt", header = TRUE, sep = ";", na.strings = "?")
filtered <- subset(rough, Date == "1/2/2007"| Date == "2/2/2007")
filtered$Date.Time <- paste(filtered$Date, filtered$Time)
final <- cbind(filtered[1:2],filtered[10], filtered[3:9])
final$Date.Time <- strptime(final$Date.Time, format = "%d/%m/%Y %H:%M:%S")

png(file = "plot4.png")
par(mfrow = c(2,2))
plot(final$Date.Time, final$Global_active_power, type = "l", xlab = "", ylab = "Global Active Power")
plot(final$Date.Time, final$Voltage, type = "l", xlab = "datetime", ylab = "Voltage")
plot(final$Date.Time, final$Sub_metering_1, type = "l", xlab = "", ylab = "Energy sub metering")
points(final$Date.Time, final$Sub_metering_2, type = "l", col = "red")
points(final$Date.Time, final$Sub_metering_3, type = "l", col = "blue")
legend("topright", c("Sub_metering_1", "Sub_metering_2", "Sub_metering_3"), lty = c(1, 1, 1), col = c("black", "red", "blue"), bty = "n")
plot(final$Date.Time, final$Global_reactive_power, type = "l", xlab = "datetime", ylab = "Global_reactive_power")
dev.off()